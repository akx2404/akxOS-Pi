# Parsing /proc for Power Estimation

## Goals
- Extract per-process CPU and memory usage.
- Log periodic snapshots for power modeling.

## Key Files
- /proc/stat
- /proc/[pid]/stat
- /proc/[pid]/status

(Will fill detailed fields once RPi setup is done.)
